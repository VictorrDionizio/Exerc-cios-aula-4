#include <stdio.h>

int main ()
{
        for (int contador = 5; contador >= 1; contador--)
        {
        printf("\n%d", contador );
        }
        printf("\nlançar");

    return 0;
}